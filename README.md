# one month

**Edit the letter:** open `letter.txt`. One paragraph per blank line. Save.

**Her name, your name, the date, photos:** the `CONFIG` block at the top of the `<script>` in `index.html`.

**Redeploy:** `git add -A && git commit -m "update" && git push` (GitHub Pages picks it up in about a minute).
